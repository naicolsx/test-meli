export type Product = {
  id: number;
  title: string;
  price: number;
  description: string;
  category: string;
  image: string;
  condition: string;
  sold_quantity: string;
  seller: { nickname: string };
};
